<h2>Editing Book</h2>
<%= form_with model: @book, local: true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>body</h4>
  <%= f.text_area :body %>
　<p><%= f.submit 'Update User' %></p>
<% end %>